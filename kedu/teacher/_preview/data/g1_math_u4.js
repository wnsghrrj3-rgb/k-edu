/* ============================================================================
   1학년 수학 4단원 (비교하기) 차시 데이터
   - 키 형식: window.LESSONS["u4_l{NN}"]
   - 현재 비어 있음. 정리 채팅이 lessons/g1_math_u4_l*.json을 가공해 누적할 자리.
   ============================================================================ */

(function () {
  if (!window.LESSONS) window.LESSONS = {};

  // 차시 누적 자리 — 예시:
  //   window.LESSONS["u4_l01"] = { meta: {...}, slides: [...], extras: [...] };
})();
